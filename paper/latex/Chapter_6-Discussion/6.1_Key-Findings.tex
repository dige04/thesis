% Generated from the matching Typst scaffold by
% scripts/migrate_typst_report_to_latex.py.

% ── 6.1  Key Findings ────────────────────────────────────────────────────
% PURPOSE: Restate the thesis-defining result up front before any interpretation.
% MOVE: Counter-intuitive lead ("Contrary to the assumption that more memory helps…").
% BEATS: see ordered bullets below — the honest story, ending on the footprint win.
% FIGURES/TABLES: fig:pareto-footprint (the robust win), fig:pareto-cost.
% CITATIONS: xiong2025; Context-Rot [VERIFY+ADD].
% SOURCE-DOC: README grounded-facts cheat sheet; docs/defense-qa.md (Q5).
% ─────────────────────────────────────────────────────────────────────────

\section{Key Findings}

\begin{todobox}
Expand the beats below into prose. Open with the COUNTER-INTUITIVE LEAD, then deliver the careful negative before the win. Thread "retrieval held constant", "non-inferiority", "footprint", "not every task needs memory". All numbers stay placeholders --- the full 144-run matrix is still RUNNING.
\end{todobox}

% - COUNTER-INTUITIVE LEAD: contrary to the assumption that accumulating memory
%   improves continual performance, memory does NOT recover lost performance on CL-F1.
% - The careful negative: with retrieval held constant, Full Memory ≈ No Memory on
%   CL-F1 — gate-3 PRELIMINARY full−nomem = −0.097 (pytest), +0.018 (django)
%   [RESULT: confirm on 144-run completion] — within noise, no recovery effect.
% - The non-inferiority result: forgetting policies MATCH full-memory accumulation on
%   CL-F1 (TOST, SESOI ±0.03) at materially lower memory footprint [RESULT: …].
% - The robust win is the FOOTPRINT axis (A4): forgetting holds CL-F1 while pruning at
%   cap=10 keeps the footprint bounded — the efficiency claim that survives the
%   stability-instrument concern (forward-ref fig:pareto-footprint).
% - Why the average benefit was always diluted: only ~⅓ of tasks structurally depend on
%   a predecessor (mean overlap 0.30, 0.32 frac with any dependency) @xiong2025 — context
%   accumulation can also rot rather than help [VERIFY+ADD] (Context-Rot).
% - One-line frame: not "forgetting recovers performance" but "forgetting is non-inferior
%   to full memory at lower footprint" — and quantifying interdependence is the contribution.
