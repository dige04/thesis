% Generated from the matching Typst scaffold by
% scripts/migrate_typst_report_to_latex.py.

% ── 6.1  Key Findings ────────────────────────────────────────────────────
% PURPOSE: Restate the thesis-defining result up front before any interpretation.
% MOVE: Counter-intuitive lead ("Contrary to the assumption that more memory helps…").
% BEATS: see ordered bullets below — the honest story, ending on the footprint win.
% FIGURES/TABLES: fig:pareto-footprint (the robust win), fig:pareto-cost.
% CITATIONS: xiong2025; Context-Rot [VERIFY+ADD].
% SOURCE-DOC: README grounded-facts cheat sheet; docs/defense-qa.md (Q5).
% ─────────────────────────────────────────────────────────────────────────

\section{Key Findings}

\begin{todobox}
Expand the beats below into prose. Open with the counter-intuitive lead, then deliver the careful negative before the footprint win. Thread "retrieval held constant", "non-inferiority", "footprint", "not every task needs memory". All numbers stay placeholders, pending the 144-run rerun.
\end{todobox}

% NEUTRAL instructions only -- no preliminary numbers (do NOT paste gate-3/old-matrix values).
% - COUNTER-INTUITIVE LEAD: contrary to the assumption that accumulating memory improves
%   continual performance, memory does not recover lost performance on the CL-F1 proxy.
% - The careful negative: with retrieval held constant, Full Memory is statistically
%   indistinguishable from No Memory on the proxy [RESULT: pending] -- state the magnitude
%   only once the 144 lands.
% - Non-inferiority: forgetting policies match full-memory accumulation on the proxy at
%   materially lower footprint [RESULT: pending]. (Non-inferiority via bounded CIs + power,
%   NOT a TOST verdict -- TOST was pre-registered but not computed.)
% - The robust win is the FOOTPRINT axis (A4): forgetting holds the proxy while pruning keeps
%   the footprint bounded (forward-ref fig:pareto-footprint).
% - Why the average benefit is diluted: only a minority of tasks structurally depend on a
%   predecessor (benchmark-structural; exact fractions pending) -- interdependence is the
%   binding filter; cite xiong2025 for the experience-following risk.
% - One-line frame: not "forgetting recovers performance" but "forgetting is non-inferior to
%   full memory at lower footprint" -- and quantifying interdependence is the contribution.
