% Generated from the matching Typst scaffold by
% scripts/migrate_typst_report_to_latex.py.

% ── 6.2  Interpretation and Implications ─────────────────────────────────
% PURPOSE: Turn the key findings into "when to forget", practitioner guidance, theory.
% MOVE: Pivot ("Rather than treat memory as always-on, treat forgetting as dosage").
% BEATS: see ordered bullets below — dosage → practitioner guidance → theory.
% FIGURES/TABLES: fig:interdependence (memory-lift by position + ~⅓ overlap).
% CITATIONS: xiong2025; anderson1991, richards2017 (transience as adaptive).
% SOURCE-DOC: README grounded-facts; docs/defense-qa.md (Q5).
% ─────────────────────────────────────────────────────────────────────────

\section{Interpretation and Implications}

\begin{todobox}
Expand the beats below into prose. Open with the PIVOT from always-on memory to forgetting-as-dosage, move to practitioner guidance, close on the cognitive-theory tie. Thread "not every task needs memory" and "footprint". Numbers stay placeholders.
\end{todobox}

% NEUTRAL instructions only -- no preliminary numbers (do NOT paste gate-3/old-matrix values).
% - WHEN TO FORGET (dosage): because only a minority of tasks are interdependent
%   (benchmark-structural; exact fractions pending), memory should be TARGETED at that
%   subset, not always-on -- "not every task needs memory" made measurable
%   (forward-ref fig:interdependence); cite xiong2025.
% - The question shifts from "store everything vs nothing" to dosage: how much memory,
%   targeted at which tasks -- forgetting is the mechanism that realizes the dosage.
% - PRACTITIONER GUIDANCE: a bounded memory budget (cap=10) with a content-aware forgetting
%   policy as the safe default -- matches full-memory on the proxy at lower footprint and
%   avoids unbounded storage growth; do not pay for accumulation that does not recover
%   performance.
% - For agents on largely file-disjoint task streams, an aggressive pruner is non-inferior;
%   reserve richer retention for high-interdependence repositories.
% - THEORETICAL IMPLICATION: transience is adaptive, not a failure mode -- cite anderson1991,
%   richards2017; our footprint-non-inferiority result is the engineering analogue.
